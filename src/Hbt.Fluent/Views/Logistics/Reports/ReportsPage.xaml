<UserControl x:Class="Hbt.Fluent.Views.Logistics.Reports.ReportsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Hbt.Fluent.Controls"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns:local="clr-namespace:Hbt.Fluent.Localization"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader
            Grid.Row="0"
            Margin="0,0,0,40"
            Title="{Binding ViewModel.PageTitle}"
            Description="{Binding ViewModel.PageDescription}" />

        <ItemsControl
            Grid.Row="1"
            Margin="-12,0,0,0"
            AutomationProperties.Name="导航卡片"
            ItemsSource="{Binding ViewModel.NavigationCards}"
            Focusable="False"
            ItemsPanel="{StaticResource WrapPanelTemplate}"
            ItemTemplate="{StaticResource NavigationCardTemplate}" />
    </Grid>
</UserControl>
