<Window x:Class="Hbt.Fluent.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:Hbt.Fluent.Helpers"
        xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
        xmlns:local="clr-namespace:Hbt.Fluent.Localization"
        xmlns:avalonDock="clr-namespace:AvalonDock;assembly=AvalonDock"
        xmlns:avalonLayout="clr-namespace:AvalonDock.Layout;assembly=AvalonDock"
        xmlns:avalonControls="clr-namespace:AvalonDock.Controls;assembly=AvalonDock"
        xmlns:avalonThemes="clr-namespace:AvalonDock.Themes;assembly=AvalonDock"
        Title="{local:Loc Key=application.title}"
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        MinWidth="780"
        MinHeight="450"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResize"
        Background="{DynamicResource LayerFillColorDefaultBrush}">
    
    <Window.Resources>
        <helpers:EmptyToVisibilityConverter x:Key="EmptyToVisibilityConverter" />
        <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <helpers:StringToIconCharConverter x:Key="StringToIconCharConverter" />
        <helpers:StringToTranslationConverter x:Key="StringToTranslationConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <helpers:FirstCharConverter x:Key="FirstCharConverter" />

        <Style x:Key="TitleBarDefaultButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="48" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="EnclosingBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlAltFillColorQuarternaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TitleBarDefaultCloseButtonStyle" BasedOn="{StaticResource TitleBarDefaultButtonStyle}" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C42B1C" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- IconBlock 默认样式，确保 IconFont 始终为 Solid，防止空字符串导致错误 -->
        <Style TargetType="fa:IconBlock">
            <Setter Property="IconFont" Value="Solid"/>
            <EventSetter Event="Unloaded" Handler="IconBlock_Unloaded"/>
        </Style>
        
        
    </Window.Resources>
    
    <Border x:Name="HighContrastBorder" BorderBrush="Transparent" BorderThickness="8 1 8 8">
        <Grid x:Name="MainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header: 标题栏 -->
            <Grid
                Grid.Row="0"
                Height="44"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" /> <!-- Logo -->
                    <ColumnDefinition Width="*" /> <!-- 标题（可选） -->
                    <ColumnDefinition Width="Auto" /> <!-- 用户信息下拉 -->
                    <ColumnDefinition Width="Auto" /> <!-- 最小化 -->
                    <ColumnDefinition Width="Auto" /> <!-- 最大化 -->
                    <ColumnDefinition Width="Auto" /> <!-- 关闭 -->
                </Grid.ColumnDefinitions>

                <!-- VS2022 样式的 Logo 容器 -->
                <Border
                    Grid.Column="0"
                    Width="48"
                    Height="32"
                    Margin="8,0,0,0"
                    Background="Transparent"
                    BorderThickness="0"
                    VerticalAlignment="Center">
                    <Border
                        x:Name="LogoContainer"
                        Width="32"
                        Height="32"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left">
                        <Image
                            Width="16"
                            Height="16"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Source="pack://application:,,,/Assets/hbt-logo.ico"
                            Stretch="Uniform"
                            RenderOptions.BitmapScalingMode="HighQuality"/>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </Border>

                <!-- 标题（可选，可隐藏） -->
                <StackPanel
                    Grid.Column="1"
                    Margin="8,0,8,0"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                        Text="{local:Loc Key=application.title}" />
                </StackPanel>

                <!-- 用户信息下拉按钮 -->
                <Button
                    x:Name="UserInfoButton"
                    Grid.Column="2"
                    Margin="0,0,0,0"
                    Padding="12,0"
                    MinWidth="48"
                    MinHeight="32"
                    HorizontalContentAlignment="Center"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="UserInfoButton_Click">
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem 
                                Header="用户信息中心" 
                                Click="UserInfoMenuItem_Click">
                                <MenuItem.Icon>
                                    <fa:IconBlock
                                        Icon="User"
                                        IconFont="Solid"
                                        Width="14"
                                        Height="14"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator />
                            <MenuItem 
                                Header="登出" 
                                Click="LogoutMenuItem_Click">
                                <MenuItem.Icon>
                                    <fa:IconBlock
                                        Icon="RightFromBracket"
                                        IconFont="Solid"
                                        Width="14"
                                        Height="14"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </Button.ContextMenu>
                    <StackPanel Orientation="Horizontal">
                        <Border
                            Width="24"
                            Height="24"
                            CornerRadius="12"
                            Background="{DynamicResource AccentFillColorDefaultBrush}"
                            Margin="0,0,8,0">
                            <TextBlock
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Foreground="White"
                                Text="{Binding ViewModel.CurrentRealName, Converter={StaticResource FirstCharConverter}}" />
                        </Border>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock
                                FontSize="12"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                Text="{Binding ViewModel.CurrentRealName}" />
                            <TextBlock
                                FontSize="10"
                                Opacity="0.7"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="{Binding ViewModel.CurrentRoleName}" />
                        </StackPanel>
                        <TextBlock
                            Margin="8,0,0,0"
                            FontFamily="{StaticResource SymbolThemeFontFamily}"
                            FontSize="10"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            VerticalAlignment="Center"
                            Text="&#xE70D;" />
                    </StackPanel>
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource TitleBarDefaultButtonStyle}">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button
                    x:Name="MinimizeButton"
                    Grid.Column="3"
                    Click="MinimizeWindow"
                    Style="{StaticResource TitleBarDefaultButtonStyle}">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                        FontSize="10"
                        Text="&#xE921;" />
                </Button>

                <Button
                    x:Name="MaximizeButton"
                    Grid.Column="4"
                    Click="MaximizeWindow"
                    Style="{StaticResource TitleBarDefaultButtonStyle}">
                    <TextBlock
                        x:Name="MaximizeIcon"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                        FontSize="10"
                        Text="&#xE922;" />
                </Button>

                <Button
                    x:Name="CloseButton"
                    Grid.Column="5"
                    Click="CloseWindow"
                    Style="{StaticResource TitleBarDefaultCloseButtonStyle}">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                        FontSize="16"
                        Text="&#xE711;" />
                </Button>
            </Grid>

            <!-- Content: 主要内容区域 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TreeView
                        x:Name="MenusList"
                        Grid.Row="1"
                        Margin="8,8,0,0"
                        AutomationProperties.Name="导航菜单"
                        ItemsSource="{Binding ViewModel.Menus}"
                        SelectedItemChanged="MenusList_SelectedItemChanged"
                        PreviewKeyDown="MenusList_PreviewKeyDown"
                        PreviewMouseLeftButtonUp="MenusList_PreviewMouseLeftButtonUp"
                        Loaded="MenusList_Loaded">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <Grid MinHeight="30">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa:IconBlock
                                        Width="16"
                                        Height="16"
                                        VerticalAlignment="Center"
                                        IconFont="Solid"
                                        Icon="{Binding Icon, Converter={StaticResource StringToIconCharConverter}}"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                        Visibility="{Binding Icon, Converter={StaticResource EmptyToVisibilityConverter}}" />
                                    <TextBlock
                                        Grid.Column="2"
                                        VerticalAlignment="Center">
                                        <TextBlock.Text>
                                            <MultiBinding Converter="{StaticResource StringToTranslationConverter}">
                                                <Binding Path="I18nKey" />
                                                <Binding Path="MenuName" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </Grid>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>

                    <DockPanel Grid.Row="2">
                        <Button
                            x:Name="SettingsButton"
                            Width="250"
                            Height="40"
                            Margin="10"
                            HorizontalContentAlignment="Left"
                            Command="{Binding ViewModel.SettingsCommand}"
                            DockPanel.Dock="Bottom">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="SettingsIcon" Margin="0,4,0,0" FontFamily="{StaticResource SymbolThemeFontFamily}">&#xE713;</TextBlock>
                                <TextBlock Margin="8,0,0,0" Text="{local:Loc Key=menu.settings}" />
                            </StackPanel>
                        </Button>
                    </DockPanel>
                </Grid>
                
                <Border
                    Grid.Column="1"
                    Margin="4,0,0,0"
                    Padding="0"
                    Background="{DynamicResource LayerFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8,0,0,0">
                    <avalonDock:DockingManager 
                        x:Name="DockingManager"
                        DocumentsSource="{Binding ViewModel.Documents}"
                        ActiveContent="{Binding ViewModel.ActiveDocument, Mode=TwoWay}"
                        DocumentClosing="DockingManager_DocumentClosing"
                        DocumentClosed="DockingManager_DocumentClosed">
                        
                        <!-- 标签页样式：确保标题正确显示 -->
                        <!-- 根据 AvalonDock 官方文档，LayoutDocumentItem 的 Model 属性指向 LayoutDocument -->
                        <avalonDock:DockingManager.LayoutItemContainerStyle>
                            <Style TargetType="{x:Type avalonControls:LayoutDocumentItem}">
                                <!-- 确保标题正确绑定到 LayoutDocument.Title -->
                                <Setter Property="Title" Value="{Binding Model.Title, Mode=OneWay}"/>
                            </Style>
                        </avalonDock:DockingManager.LayoutItemContainerStyle>
                        
                        <!-- 设置内容模板，确保 LayoutDocument.Content 正确显示 -->
                        <!-- 根据错误日志，DataContext 直接是 AvalonDocument（LayoutDocument），不是 LayoutItem -->
                        <avalonDock:DockingManager.LayoutItemTemplate>
                            <DataTemplate>
                                <!-- 直接绑定到 Content（因为 DataContext 是 LayoutDocument，有 Content 属性） -->
                                <ContentPresenter Content="{Binding Content}"/>
                            </DataTemplate>
                        </avalonDock:DockingManager.LayoutItemTemplate>
                        
                        <avalonDock:DockingManager.Layout>
                            <avalonLayout:LayoutRoot>
                                <avalonLayout:LayoutPanel Orientation="Horizontal">
                                    <avalonLayout:LayoutDocumentPaneGroup>
                                        <avalonLayout:LayoutDocumentPane x:Name="MainDocumentPane" />
                                    </avalonLayout:LayoutDocumentPaneGroup>
                                </avalonLayout:LayoutPanel>
                            </avalonLayout:LayoutRoot>
                        </avalonDock:DockingManager.Layout>
                    </avalonDock:DockingManager>
                </Border>
            </Grid>

            <!-- Footer: 状态栏 -->
            <Border
                Grid.Row="2"
                Height="24"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock
                        Grid.Column="0"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{local:Loc Key=Common.Copyright}" />
                    
                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal"
                        Margin="0,0,8,0">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{Binding ViewModel.ActiveDocumentTitle}"
                            Margin="0,0,16,0" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
