<UserControl x:Class="Hbt.Fluent.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:Hbt.Fluent.Helpers"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <helpers:IntToBoolConverter x:Key="IntToBoolConverter" />
        <helpers:IntToVisibilityConverter x:Key="IntToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TabControl 
            Grid.Row="0"
            ItemsSource="{Binding ViewModel.SettingCategories}"
            SelectedItem="{Binding ViewModel.SelectedCategory, Mode=TwoWay}">
            <TabControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1"/>
                </ItemsPanelTemplate>
            </TabControl.ItemsPanel>
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </TabControl.Resources>
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Category}" HorizontalAlignment="Center"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ScrollViewer 
                        Margin="0,0,0,24" 
                        Padding="0,0,24,0" 
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Disabled">
                        <Grid Margin="24,16,24,24" HorizontalAlignment="Stretch">
                            <ItemsControl ItemsSource="{Binding Settings}" HorizontalAlignment="Stretch">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel HorizontalAlignment="Stretch"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border 
                                            Background="{DynamicResource ExpanderHeaderBackground}" 
                                            BorderBrush="{DynamicResource ExpanderHeaderBorderBrush}" 
                                            BorderThickness="1" 
                                            Padding="16" 
                                            CornerRadius="8"
                                            Margin="0,0,0,12"
                                            MinHeight="80"
                                            HorizontalAlignment="Stretch">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                
                                                <Grid Grid.Row="0" Margin="0,0,0,12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" MinWidth="0" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock 
                                                            Text="{Binding SettingKey}" 
                                                            FontSize="14" 
                                                            FontWeight="SemiBold" />
                                                        <TextBlock 
                                                            Text="{Binding SettingDescription}" 
                                                            Opacity="0.7" 
                                                            FontSize="12" 
                                                            Margin="0,4,0,0"
                                                            TextWrapping="Wrap" />
                                                    </StackPanel>
                                                    
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Top">
                                                        <TextBlock 
                                                            Text="内置" 
                                                            FontSize="12" 
                                                            Opacity="0.6"
                                                            Visibility="{Binding IsBuiltin, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=0}"
                                                            Margin="0,0,8,0" />
                                                        <TextBlock 
                                                            Text="只读" 
                                                            FontSize="12" 
                                                            Opacity="0.6"
                                                            Visibility="{Binding IsEditable, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=1}"
                                                            Margin="0,0,8,0" />
                                                    </StackPanel>
                                                </Grid>
                                                
                                                <Grid Grid.Row="1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" MinWidth="0" />
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <TextBlock 
                                                        Grid.Column="0"
                                                        Text="设置值：" 
                                                        FontSize="12"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,12,0"
                                                        MinWidth="60" />
                                                    
                                                    <TextBox 
                                                        Grid.Column="1"
                                                        Text="{Binding SettingValue, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                                        IsReadOnly="{Binding IsEditable, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"
                                                        LostFocus="SettingValueTextBox_LostFocus"
                                                        Tag="{Binding}"
                                                        IsEnabled="{Binding IsEditable, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}"
                                                        MinHeight="32"
                                                        VerticalContentAlignment="Center" />
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</UserControl>
