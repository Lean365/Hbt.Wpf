<UserControl x:Class="Takt.Fluent.Views.Logistics.Materials.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Takt.Fluent.Controls"
             xmlns:local="clr-namespace:Takt.Fluent.Extensions"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <controls:TaktDataGrid ItemsSource="{Binding ProdMaterials}"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                                SelectedItem="{Binding SelectedProdMaterial, Mode=TwoWay}"
                                QueryKeyword="{Binding Keyword, Mode=TwoWay}"
                                QueryPlaceholder="{local:Localization Key=common.placeholder.keywordHint, Param0Key=logistics.materials.prodmaterial.query.keyword}"
                                ShowToolbarCreate="True"
                                ShowToolbarUpdate="True"
                                ShowToolbarDelete="True"
                                ShowToolbarImport="True"
                                ShowToolbarExport="True"
                                CreateCommand="{Binding CreateCommand}"
                                UpdateCommand="{Binding UpdateCommand}"
                                DeleteCommand="{Binding DeleteCommand}"
                                ImportCommand="{Binding ImportCommand}"
                                ExportCommand="{Binding ExportCommand}"
                                DownloadTemplateCommand="{Binding DownloadTemplateCommand}"
                                RowUpdateCommand="{Binding UpdateCommand}"
                                RowDeleteCommand="{Binding DeleteCommand}"
                                PageIndex="{Binding PageIndex, Mode=TwoWay}"
                                PageSize="{Binding PageSize, Mode=TwoWay}"
                                TotalCount="{Binding TotalCount}"
                                QueryCommand="{Binding QueryCommand}"
                                ResetCommand="{Binding ResetCommand}"
                                PageChangedCommand="{Binding PageChangedCommand}"
                                EmptyText="{Binding EmptyMessage}">
            <controls:TaktDataGrid.Columns>
                <controls:TaktDataGridColumnDefinition Header="{local:Localization Key=logistics.material.materialcode}" BindingPath="MaterialCode" Width="150" />
                <controls:TaktDataGridColumnDefinition Header="{local:Localization Key=logistics.material.materialdescription}" BindingPath="MaterialDescription" Width="200" />
                <controls:TaktDataGridColumnDefinition Header="{local:Localization Key=logistics.material.materialtype}" BindingPath="MaterialType" Width="120" />
                <controls:TaktDataGridColumnDefinition Header="{local:Localization Key=logistics.material.plant}" BindingPath="Plant" Width="100" />
                <controls:TaktDataGridColumnDefinition Header="{local:Localization Key=logistics.material.baseunit}" BindingPath="BaseUnit" Width="100" />
                <controls:TaktDataGridColumnDefinition Header="{local:Localization Key=logistics.material.stockquantity}" BindingPath="StockQuantity" Width="120" />
            </controls:TaktDataGrid.Columns>
        </controls:TaktDataGrid>
    </Grid>
</UserControl>

