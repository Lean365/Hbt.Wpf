<Window x:Class="Takt.Fluent.Views.Routine.DictionaryComponent.DictionaryTypeForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Takt.Fluent.Localization"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:helpers="clr-namespace:Takt.Fluent.Helpers"
        xmlns:controls="clr-namespace:Takt.Fluent.Controls"
        mc:Ignorable="d"
        d:DesignHeight="500"
        d:DesignWidth="600"
        Title="{Binding Title}" 
        SizeToContent="Manual" 
        WindowStartupLocation="Manual"
        ResizeMode="CanResize" 
        Background="{DynamicResource MaterialDesignPaper}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <helpers:IntEqualsConverter x:Key="IntEqualsConverter"/>
        <helpers:EmptyToVisibilityConverter x:Key="EmptyToVisibilityConverter"/>
        <helpers:StringToBoolConverter x:Key="StringToBoolConverter"/>
        <helpers:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="TextAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">
                <!-- 类型代码 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Routine.Dictionary.TypeCode}"/>
                        </TextBlock>
                        <controls:TaktTextBox Grid.Column="1" 
                                                    Size="Small"
                                                    VerticalAlignment="Center"
                                                    Text="{Binding TypeCode, UpdateSourceTrigger=PropertyChanged}"
                                                    HasError="{Binding TypeCodeError, Converter={StaticResource StringToBoolConverter}}"
                                                    IsReadOnly="{Binding IsCreate, Converter={StaticResource InverseBoolConverter}}"/>
                    </Grid>
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ErrorTextStyle}" 
                               Text="{Binding TypeCodeError}"
                               Visibility="{Binding TypeCodeError, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                </StackPanel>

                <!-- 类型名称 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Routine.Dictionary.TypeName}"/>
                        </TextBlock>
                        <controls:TaktTextBox Grid.Column="1" 
                                                    Size="Small"
                                                    VerticalAlignment="Center"
                                                    Text="{Binding TypeName, UpdateSourceTrigger=PropertyChanged}"
                                                    HasError="{Binding TypeNameError, Converter={StaticResource StringToBoolConverter}}"/>
                    </Grid>
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ErrorTextStyle}" 
                               Text="{Binding TypeNameError}"
                               Visibility="{Binding TypeNameError, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                </StackPanel>

                <!-- 排序号 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{local:Loc Key=Routine.Dictionary.OrderNum}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0"/>
                        <controls:TaktTextBox Grid.Column="1" 
                                                    Size="Small"
                                                    VerticalAlignment="Center"
                                                    Text="{Binding OrderNum, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0}'}"
                                                    HasError="{Binding OrderNumError, Converter={StaticResource StringToBoolConverter}}"/>
                    </Grid>
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ErrorTextStyle}" 
                               Text="{Binding OrderNumError}"
                               Visibility="{Binding OrderNumError, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                </StackPanel>

                <!-- 类型状态 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Routine.Dictionary.TypeStatus}"/>
                        </TextBlock>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <controls:TaktRadioButton Content="{local:Loc Key=common.enabled}"
                                                            Size="Small"
                                                            GroupName="TypeStatusGroup"
                                                            VerticalAlignment="Center"
                                                            IsChecked="{Binding TypeStatus, Converter={StaticResource IntEqualsConverter}, ConverterParameter=0}"/>
                            <controls:TaktRadioButton Content="{local:Loc Key=common.disabled}"
                                                            Size="Small"
                                                            GroupName="TypeStatusGroup"
                                                            VerticalAlignment="Center"
                                                            Margin="12,0,0,0"
                                                            IsChecked="{Binding TypeStatus, Converter={StaticResource IntEqualsConverter}, ConverterParameter=1}"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ErrorTextStyle}" 
                               Text="{Binding TypeStatusError}"
                               Visibility="{Binding TypeStatusError, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                </StackPanel>

                <!-- 备注 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{local:Loc Key=common.audit.Remarks}" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,12,0"/>
                        <StackPanel Grid.Column="1">
                            <controls:TaktTextBox 
                                Size="Medium"
                                Text="{Binding Remarks}"
                                TextWrapping="Wrap" 
                                AcceptsReturn="True" 
                                VerticalScrollBarVisibility="Hidden"
                                MinHeight="80"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button IsDefault="True" 
                    Command="{Binding SaveCommand}" 
                    Content="{local:Loc Key=common.button.submit}" 
                    Style="{StaticResource DefaultPrimary}"
                    Margin="0,0,12,0"/>
            <Button IsCancel="True" 
                    Content="{local:Loc Key=common.button.cancel}"
                    Style="{StaticResource DefaultInfo}"/>
        </StackPanel>
    </Grid>
</Window>

