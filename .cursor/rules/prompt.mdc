---
alwaysApply: true
---

# 提示语编写规范

## 标准结构

**强制要求**：所有提示语必须遵循以下结构

```markdown
[任务类型] [目标描述]

**相关代码**：
- @文件路径:行号（必须提供）

**需求描述**：
- 功能需求：[具体功能点]
- 业务规则：[业务约束]

**约束条件**：
- 技术约束：[技术限制]
- 规范约束：[必须遵循的规范]

**验证要求**：
- 必须基于真实代码验证
- 必须提供日志输出证明
- 必须明确信息来源
```

## 任务类型标签

**标准任务类型**（必须使用）：

- `[功能实现]` - 实现新功能
- `[问题修复]` - 修复 Bug
- `[代码重构]` - 重构现有代码
- `[性能优化]` - 优化性能
- `[文档更新]` - 更新文档
- `[测试编写]` - 编写测试
- `[代码审查]` - 审查代码质量

## 完整示例

```markdown
[问题修复] 修复标签页图标不显示的问题

**相关代码**：
- @src/Hbt.Fluent/Views/MainWindow.xaml:367-383（DockingManager 配置）
- @src/Hbt.Fluent/Models/AvalonDocument.cs:60-138（AvalonDocument 构造函数）

**需求描述**：
- 功能需求：标签页必须显示图标（图标 + 标题 + 关闭按钮）
- 业务规则：图标必须与菜单树中的图标一致

**约束条件**：
- 技术约束：必须使用 AvalonDock 4.72.1 官方 API
- 规范约束：必须遵循 MVVM 模式，不能破坏现有功能

**验证要求**：
- 必须基于真实代码验证（@文件路径:行号）
- 必须提供日志输出证明
- 必须参考 AvalonDock 官方文档：https://github.com/Dirkster99/AvalonDock/wiki
```

## 禁止事项

- ❌ 推测性结论（"可能"、"应该"、"也许"）
- ❌ 未经验证的假设
- ❌ 模棱两可的答案
- ❌ 基于通用知识而非项目代码的结论
